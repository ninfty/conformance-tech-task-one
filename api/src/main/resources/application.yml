server:
  port: 8080

spring:
  security:
    oauth2:
      resourceserver:
        opaque-token:
          introspection-uri: http://localhost:8081/token/introspect
          client-id:
          client-secret: